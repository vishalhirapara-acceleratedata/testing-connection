name: 'data_engineering'
version: '1.0.0'
config-version: 2
profile: 'ecommerce'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

seeds:
  data_engineering:
    +schema: raw

models:
  data_engineering:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
